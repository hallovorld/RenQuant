# 2026-05-02 main-branch deep audit

Audit covering test suite, doc-code alignment, and silent-failure scan.
Conducted while Stage 3 wl-expansion experiment runs in background (~10h wallclock).

## ✅ Test suite

```
2929 passed, 10 skipped, 0 failed in 41.67s
```

**Fixed pre-audit**: 1 stale test `test_triple_barrier_mode_skips_residualization`
asserted v1 behavior (skip residualization) but code is on v2 (hit-time-matched).
Updated assertion to verify v2 wiring (`compute_residual_returns_hit_aligned` +
`hit_days_by_ticker`). 13 Track F tests now green.

## ✅ Critical doc currency

Three top-level docs aligned with current main state:

| Doc | Stale → Updated |
|---|---|
| `CLAUDE.md` | "+0.0418 IC, 15-fold, 2330 tests" → "+0.034, 6-fold, 2929 tests"; added E22/E23/E25 closures + Track D status + §5.2 sanity 三件套 visible at top |
| `doc/STATUS.md` | "60d XGBoost +110%" callout flagged for re-validation under today's σ; production state synced to 2026-04-30 retrain + Track D in-flight |
| `doc/arch/strategy-104.md` | "28 features × 99 tickers × 74,547 rows × OOS 0.0482" → "27 × 103 × 77,559 × +0.034"; added shelved-experiment cross-refs + Stage 3 status |

## ✅ Path / class reference integrity

Verified all 35 file paths referenced in CLAUDE.md exist. Verified all 14
referenced Pipeline / Job classes (InferencePipeline, FullTrainingPipeline,
PanelTrainingPipeline, LoadUniverseJob, etc) are defined in code at the
expected modules.

## ✅ Code-level findings

### Silent exception swallowers — none unsafe
Scanned `kernel/`, `training_panel/`. Found patterns matching `except ...:` —
all are typed exceptions (ImportError, sqlite3.OperationalError, json.JSONDecodeError,
ValueError, TypeError) with explicit handling. No blanket `except Exception: pass`
that would hide real failures.

### Guard escape clauses verified consistent
`min_best_iter_eval_ic_floor` (BUG-CV-2 escape) referenced in code (3×),
test (3×), CLAUDE.md (1×). Consistent.

## ⚠️ Known stale, NOT updated this audit

These are **point-in-time snapshots** by design — their date IS the context.
Do NOT rewrite them:

- `doc/assessments/2026-04-28-system-assessment*.md`
- `doc/components/full-training-deep-audit-2026-04-27.md`
- `doc/components/lgbm-implementation-audit.md`
- `doc/components/macro-factor-frame-redesign.md`
- `doc/components/t2-4-and-macro-v2-deep-audit-2026-04-27.md`
- `doc/components/asset-embeddings-design.md` (E20-shelved per failed-experiments-log)
- `doc/research/hourly-workflow-feasibility.md` (one-shot research note)
- `doc/research/macro-data-expansion-plan-2026-04-27.md`

These docs reference the +0.0418 / 99-ticker / 28-feature numbers; that's
correct for their dated context. Future audits should add "STATUS:
SUPERSEDED" headers where appropriate but not edit the body.

## ⚠️ Living docs flagged but not yet updated

Lower-priority active docs that could drift:
- `doc/roadmap.md` (header still says "v4.1 sweep ≈ 39.82%"; need re-sim to confirm)
- `doc/components/calibration.md` (references stale baseline IC)
- `doc/components/transformer.md` (references stale per-ticker model count)
- `doc/components/training-data-format.md` (references stale watchlist size)

If these become consult-targets again, update their header sections.

## 🔄 Side-config sprawl

This session created 12+ side strategy configs (insider_off / insider_on /
insider_on_diag / pead_off / pead_on / pead_off_run2 / pead_off_run3 /
pead_days_only / pead_days_only_diag / triple_barrier_off / triple_barrier_on /
triple_barrier_on_repro / triple_barrier_on_shuffled / triple_barrier_on_placebo /
fwd5d_placebo_shift60 / + Stage 3 batch configs). All committed for repro.
Considered cleanup but kept — they serve as living test of the
side-config artifact-path invariant guard (17 tests pass on all of them).

## Recommendations for future audits

1. Add `STATUS: SUPERSEDED YYYY-MM-DD` headers to truly outdated docs (so
   greppable + clear without rewriting body).
2. Make `failed-experiments-log.md` E-numbers cross-referenced from any doc
   that mentions a now-shelved experiment (currently inconsistent — some docs
   reference E22-E25, others still discuss the experiment as if active).
3. Bake in a quarterly "doc currency check" as a routine task — each
   actively-consulted doc should have a "Last verified YYYY-MM-DD" stamp.

## Summary

- 1 broken test → fixed (Track F v2 alignment).
- 3 critical docs → aligned with code.
- 35 path references → all valid.
- 14 Pipeline class references → all valid.
- No silent failure patterns found.
- 8 historical docs flagged as point-in-time (do not rewrite).
- 4 living docs flagged for future update.

Main branch is **clean per audit dimensions checked**. Production model
artifacts unchanged. Stage 3 expansion experiment continues in background.
