# 2026-05-17 NIGHT — detector + σ-wire + 3 prod-corruption gates + HIFO + 10 commits

Archived from CLAUDE.md `🗂 Status` block on 2026-05-19.

**10 commits shipped, all on `origin/main`:**

**Detector + per-regime σ-wire infrastructure (commits `0a192c4`, `e267101`):**
- 5-day BEAR trigger (vol > 25% / ret < −4%) + vol-cluster CHOPPY
  (vol_5d > vol_60d×1.5 ∧ |drift_20d| < 2%). Catches SVB / DeepSeek /
  Aug-2024 crises the 20-day rule missed. Audit at
  `data/logs/reeval_results/regime_detector_audit_2026-05-17.txt`.
- Per-regime NGBoost σ-wire kernel patch in
  `job_panel_scoring.py::_ngb_cfg`. `regime_params.<R>.ngboost.{enabled,
  score_mode, lambda_sigma}` overrides global. Hysteresis sticky N=10
  bars in `task_regime.py::RegimeFinalizeTask` prevents detector-flicker
  churn (added but currently NOT effective — see σ-wire A/B verdict).
- `LoadNGBoostTask` now uses `_ngb_cfg` (raw-config read was bypassing
  per-regime activation pre-fix).
- State-EXT-SELL pending-order false-positive fix: HON (today) and
  META (5/15) both got 30-day wash-sale blocked because a Sunday-queued
  BUY order whose Alpaca status=accepted wasn't filled was misclassified
  as "externally sold". Fix: exclude `pending_broker_tickers` from the
  disappeared set.
- P-FEATURE-COVER preflight per-regime gap: skipped entirely when
  global enabled=false, missing the per-regime activation case. Now
  scans `regime_params.*.ngboost.enabled` too.

**σ-wire A/B (3 conditions, ALL NULL/negative; σ wire stays OFF in golden):**
- global σ-on (8 dense windows): mean Δ=+3.01pp NULL (CI crosses 0)
- per-regime σ-on (BEAR/CHOPPY/BULL_VOL): mean Δ=−4.70pp negative
- per-regime + hysteresis: mean Δ=−7.89pp negative (worse — regime
  flicker × σ-wire toggle = churn)
- Per-regime overlay ADDED to golden as dormant infrastructure for
  future re-evaluation; today's regime is BULL_CALM so it's a no-op.

**NGB head retrain + prod promote (commit `e267101`):**
- `train_ngboost_proper.py` extended: save best-by-val_IC + quality
  gate refusing save when val_IC < XGB baseline +0.0294 (§5.13.15
  missing-gate fix).
- seed=42 today: val_IC=+0.0352, σ-calib=+0.274, best_iter=36 →
  PASSED gate → promoted to prod (md5 `30b0460a`; .bak rehearsal
  verified). σ wire stays OFF per A/B above; NGB μ predictions
  available but not consumed.

**3 prod-corruption-class fixes:**
1. `fix(buy-gates,qp)` `d318060`: DDV disabled globally per HXZ 2020
   "Replicating Anomalies" (RFS) — distress/loser anomaly fails to
   replicate in modern data + Fama-French 2008 large-cap exemption.
   Today's incident: DDV vetoed META (rank #1, μ=+0.146) on dd=-22.2%.
   Author's own docstring warned costs +25pp in BULL_CALM.
   + `min_share_floor` for high-price stocks (EQIX-class): when
   target_w × NAV < share_price, allow buying 1 share if 1-share-weight
   in [5%, 15%]. Unblocked EQIX ($1059/share).
2. `fix(sunday-sweep)` `477b94c`: pre-sweep backup + per-backend
   acceptance gate (H1-H4: pool_ic exists, ≥0, ≤2pp drop, scorer ≥0)
   + best-by-OOS-IC selection. Today's Sunday sweep wrote NGB
   val_IC=−0.0165 + 21-feat panel-LTR stub straight to prod with no
   gate.
3. `fix(monthly-cal)` `637594e`: same pattern — pre-refit backup,
   H2a non-collapse hard gate (n_unique_prob_y ≥ 10, was display-only),
   H2b IC-regression hard gate (pool_ic drop > 2pp), rollback on fail.

**Walk-forward gate enforcement (commit `96af42b`):**
- Removed `RQ_ALLOW_NO_WF=1` setdefault from `train_104.py`. §5.13.15
  "every daily promote set RQ_ALLOW_NO_WF=1 — theatrical gate". Daily
  retrain now STAGES only; weekly_wf_promote.sh (Saturday 04:00 PT)
  does the actual promote with full WF + sanity battery. Emergency
  override via shell-env `RQ_ALLOW_NO_WF=1` still works.

**HIFO lot-selection default (commit `bc18795`):**
- `qp_tax_lot_method` flipped FIFO → HIFO in golden + live config.
  HIFO is tax-optimal across both regimes (smallest gain when selling
  up, biggest harvest when selling down). Pure lot accounting change,
  not tax-driven sell logic (`feedback_no_tax_driven_logic`-safe).
  Industry standard (Wealthfront, M1 Finance, Schwab Intelligent).

**Long-short pre-req empirical gate (commit `28251c2`):**
- Roadmap #0 test: model bottom decile 60d-ann = +0.58% (positive!),
  top = +17.77%, spread +17.19%. All alpha on LONG side; SKIP
  verdict (Kelly-Gu-Xiu 2020 RFS standard: short alpha needs
  −10% to −15%/yr). **Saves 3-4 weeks engineering**.

**Vol-adjusted label retest (commit `96af42b`):**
- Roadmap #5 NEGATIVE: vol-normalized label val_IC=−0.0189 vs raw
  +0.0352 (Δ=−0.054). Hypothesis rejected by quality gate.

**3 LIVE orders queued for Monday open** (real Alpaca account):
- BUY HON 2 shares @ $213.24 = $426  (placed 15:54 PT)
- BUY EQIX 1 share @ $1059.44       (placed 22:23 PT, min_share_floor)
- BUY META 1 share @ $614.23        (placed 22:23 PT, DDV disabled)
- Total $2099 / $10557 acct = ~20% rebalance on Monday open.

**Tests: 11747 passed / 12 pre-existing failures / 7897 skipped**
(3 regression tests updated for today's task-count / wf-bypass / BEAR
refactor — commit `46e3333`).

**e2e = LIVE Alpaca account (real money) — user mandate locked in**
(CLAUDE.md Environment §"e2e" + `feedback_e2e_means_real_broker.md`).
