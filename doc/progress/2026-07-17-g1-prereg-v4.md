# Progress: G1 equal-weight prereg v5 (standalone)

Date: 2026-07-17

## What

`doc/experiments/2026-07-17-equal-weight-deployment-prereg-v5.md` — the
complete, SELF-CONTAINED G1 pre-registration. Replaces the earlier
amendment-style drafts in this PR (v4/v5 amendment files deleted): the
v3 base they amended was reverted from main by #491, so amendments had
nothing in-tree to amend (review round 1, P1-1).

## Decision-rule lineage

- v3: LB90 > MDE — structurally unsatisfiable (power at own design point
  ≈ α; proven by the #485 simulation).
- v4: LB90 > 0 — rejected: statistical positivity is not a deployment
  criterion.
- v5: statistical efficacy (LB > 0) REPORTED; deployment eligibility
  (LB > MEE = 3 bps) DECIDES; power sized at PE = 6 bps; type-I
  evaluated for the deployment rule at μ = MEE.

## Data hygiene (review round 1, P1-2)

§4.7 hard rules: pre-freeze/already-analyzed sessions (incl. the ~4
unblinded pairs behind the #485 provisional σ_d) permanently burned from
both pilot and terminal series; cross-epoch pooling forbidden (pilot and
terminal each single-epoch; epoch break mid-pilot restarts it, mid-terminal
invalidates unless T already reached); blinded ≥40-session calibration
pilot with sealed orientation; per-epoch/per-role session accounting in
harness telemetry for mechanical auditability.

## Status

RFC only — no activation, no capital, no observation start. Reviewer:
the claude identity (author = codex identity).
