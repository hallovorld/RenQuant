# Snapshot provenance fixes for the strategy-104 pin advance   (PR #524)

STATUS:    in-progress
WHAT:      Advances the strategy-104 subrepo pin (082dccd -> 1e840cd, PatchTST
           shadow-path fix #62 + shadow watchlist #58) and fixes the
           pre-deploy snapshot renderer
           (`scripts/render_strategy_104_snapshot.py`) so it cannot go green
           over a candidate whose scorer provenance is silently `unknown` or
           whose fingerprint depends on mutable live state:
           1. One resolver, same as runtime (was a second, drifting
              resolver): `_resolve_artifact_path` now delegates to
              renquant-pipeline's canonical `kernel.artifact_resolver.locate_artifact`
              (imported from the pinned checkout) — the same
              `absolute -> strategy_dir -> repo_root` order the loader and
              the pre-deploy CI gate (#525) use. Declaration-only CI (no
              `.subrepo_runtime`) falls back to a display-only strategy_dir
              join.
           2. Required-scorer provenance can no longer be silently
              `unknown`: a CONFIGURED scorer (active / in-run shadow /
              shadow-e2e) that fails to resolve to a metadata-bearing file
              now emits a `SCORER PROVENANCE UNRESOLVED` warning.
           3. Re-fit calibrators are runtime observations, not part of the
              candidate fingerprint: no longer folded into the `sources` set
              that feeds the Source fingerprint (digests still render,
              under an explicit "runtime observation, excluded" note), and
              `--source-pin` renders the snapshot as a pre-deploy
              declaration from a candidate lock checkout rather than the
              live runtime HEAD.
WHY/DIR:   Codex round-2 CR on this PR found the snapshot could report a
           pin as pre-deploy-clean while (a) the PatchTST shadow scorer's
           provenance rendered `unknown` (resolver joined only
           `strategy_dir/ref`, missing the umbrella-root ref the daily
           loader actually resolves) and (b) the fingerprint absorbed the
           mutable live-refit calibrator instead of the pinned candidate —
           both would let a pin advance past the snapshot gate without
           proving the deployed scorer is the one actually traceable.
EVIDENCE:  artifact:      doc/arch/strategy-104-snapshot.md (regenerated by
                          the fixed renderer)
           prod or exp:   n/a — CI/snapshot-renderer contract change, no
                          model/data performance claim
           existing data: n/a
           best-known?:   n/a
           scope:         "this is a doc-generator + committed-doc fix,
                          not a performance claim; correctness is proven by
                          `--check`/`--verify-pinned-declaration` exiting 0
                          and the test suite, not by any IC/Sharpe number"
           `scripts/render_strategy_104_snapshot.py --selftest` -> SELFTEST
           OK. `python3 -m pytest tests/test_render_strategy_104_snapshot.py`
           -> 16 passed. Fix-pass 2: regenerated
           `doc/arch/strategy-104-snapshot.md` against the live
           `.subrepo_runtime` (pin-aligned to 1e840cd) + live artifacts ->
           `render_strategy_104_snapshot.py --check` exits 0 and
           `--verify-pinned-declaration` -> OK (both were failing/stale on
           the prior head). [VERIFIED]
           Fix-pass 3 (this update, rq-fix): #525
           (`config-artifact-path-gate`) merged into `main` on
           2026-07-22 (commit `a799602`/`8406548`), so the precondition in
           NEXT item 1 below is now satisfied — but the gate's
           `verify-pinned-paths` job (CI run 29964105583) FAILED on the
           current head: it clones `renquant-pipeline` at this PR's locked
           commit and the gate hard-requires renquant-pipeline#211's
           `shadow_health.resolve_artifact_identity` contract; the locked
           commit (`d32f701`, #208) predates #211 (`05eaf8f`), so the
           import fails closed (`No module named
           'renquant_pipeline.kernel.panel_pipeline.shadow_health'`) —
           exactly the failure mode the gate's own workflow comment names
           ("a pin whose pipeline predates #211 fails closed ... the #211
           rollout advances that pin"). Fix: bumped the
           `renquant-pipeline` pin in `subrepos.lock.json` from `d32f701`
           to `05eaf8f` (#209 + #211, both additive/non-breaking — no
           other subrepo pin needs to move) via
           `scripts/promote_pin.py bump --subrepo renquant-pipeline
           --commit 05eaf8f829f9bd42a8162c90cb7c9e066ac85d43 --apply`,
           regenerated the now-stale snapshot via `make snapshot`, and
           reproduced the CI job locally against the new pin:
           `PYTHONPATH=.subrepo_runtime/repos/renquant-pipeline/src
           python3 scripts/check_config_artifact_paths.py --registry
           scripts/config_artifact_gate_registry.json --configs-dir
           .subrepo_runtime/repos/renquant-strategy-104/configs
           --strategy-dir backtesting/renquant_104 --data-root .` ->
           `OK: all 16 configured artifact path(s) resolve + identify.`
           (exit 0). `render_strategy_104_snapshot.py --check` and
           `--verify-pinned-declaration` both still exit 0 post-bump.
           `pytest tests/test_render_strategy_104_snapshot.py
           tests/test_check_config_artifact_paths.py` -> 36 passed, 1
           skipped. [VERIFIED locally; awaiting the hosted GitHub Actions
           re-run on push for final confirmation]
NEXT:      1. RESOLVED (this update): #525 merged and the artifact gate
              now passes against this exact lock candidate (pipeline pin
              bumped to include #211; see EVIDENCE fix-pass 3). Awaiting
              the hosted CI re-run on push to confirm the same result
              off-machine before merge.
           2. RESOLVED (fix-pass 2): the committed
              `doc/arch/strategy-104-snapshot.md` was rendered by the OLD
              renderer and failed `--check`; the runtime pin-align to
              1e840cd was already deployed on the operator machine, so the
              doc is now regenerated from that live `.subrepo_runtime` +
              artifacts and round-trips clean.
           Not merging in this pass (rq-fix scope is fix-and-push, not
           merge; §3.0 forbids self-merge without the other agent's
           `APPROVED` review at the current head anyway).
