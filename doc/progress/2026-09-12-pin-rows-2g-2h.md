# Pin advance: rows 2g + 2h — the reviewed twin of the 2026-09-12 containment landing   (PR #TBD)

STATUS:    reviewed surface for a live pin edit already applied under CLAUDE.md §5.
WHAT:      `subrepos.lock.json`: renquant-pipeline faf1416a34 → 235d3c5e2a (pipeline#310
           head, A4-T1 window extension to 2026-09-28); renquant-strategy-104
           799821245b → d2f27e2879 (s104#107 head, components[0].expected_content_sha256
           6461b827ab2339a8 → f1b1c1322e3b66f7 across 7 carriers). Each head is its pin
           plus ONLY that PR's commits (merge-base == pin). Nothing else changes.
WHY/DIR:   Operator authorization 2026-09-12 (verbatim in LONG rows 2g/2h, orch#1115/#1118).
           The merge gate is out on quota until 2026-10-03 and the served artifact ages out
           2026-09-28, so the fix was landed on the live tree by editing this exact file and
           re-running `subrepo_assemble.py --sync`. Containment protocol (c) requires the
           reviewed surface to carry the identical change in the same batch — this PR is
           that. It is byte-identical to the live edit.
EVIDENCE:  artifact:      readonly full-funnel preflight sim on a scratch runtime assembled from this lock, live artifacts read-only: pin identity OK 9/9; `P-WF-GATE ✓ LICENSED`; `P-REGIME-IC ✓ LICENSED (RFC#210 A4-T1)`; blend loaded (no `panel_scorer_load_failed`); `funnel integrity: verdict=ECONOMIC_TRADE candidates_final=70 buys=5`; attestation `no writes, no notify, decision reached`; analyzer `dawn funnel preflight OK` [VERIFIED — 2026-09-12 11:02–11:03 PDT]
           prod or exp:   this file IS the prod pin surface; the live tree already carries this edit (containment record: renquant-orchestrator `doc/progress/2026-09-12-containment-landing-rows-2g-2h.md`, with the literal revert)
           existing data: pipeline#310 CI green; s104#107 104 passed; pins point at commits that exist on origin (pushed PR branches), which is all `subrepo_assemble.py --sync` requires (commit-only `_is_pinned`, fetch + detached checkout)
           best-known?:   n/a — pin plumbing; the served model's edge is unchanged (genuine_ic ~0.0016)
           scope:         "two commit strings in one file; no other file"
NEXT:      merge order once codex returns: pipeline#310 → s104#107 → this PR (their heads are then on main, so the pins are main commits) → live ff-only sync lifts the containment. If either PR is revised before merge, this PR's commits must be re-pointed to the new heads AND the live lock re-edited the same way.
