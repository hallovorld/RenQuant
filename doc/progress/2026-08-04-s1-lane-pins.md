# 2026-08-04 — S1 lane pin batch: strategy-104 profile + pipeline#261 together

## Bottom line

TWO-STEP LANDING, declared (codex on s104#86: the reviewable coordinated
batch must EXIST before the profile unblocks; codex on s104#85: an
unmerged-branch pin must never MERGE): this PR opens pinning the OPEN
s104#86 head `2215a0e2` so the exact strategy commit + pipeline
`3ecd9880` + candidate gate + regenerated snapshot are reviewable
TOGETHER. It merges only AFTER s104#86 merges, with one final commit
bumping the s104 pin line to the squash sha + a snapshot re-render —
main never carries an unmerged-branch pin.

The coordinated landing the frozen S1 prereg requires (orch#777; s104#85
round 1): strategy-104 (the shadow_blend_momentum profile) and pipeline
(3ecd9880, carrying #261's blend kind dispatch) advance IN THE SAME
COMMIT, so the profile is never an inert config on the deployed interface
and the loader it needs is pinned in the same breath. RQ#563's Step 5b
rail is already on umbrella main and reaches the live tree in this
batch's deployment pull.

## Pin advances (2)

| subrepo | from | to | carries |
|---|---|---|---|
| renquant-strategy-104 | `e8fd07e9` | `d84604d7` (the s104#86 SQUASH sha — landing step 2 executed) | the S1 profile + mirrored semantic-pin test + lane-true admission notes |
| renquant-pipeline | `936869f8` | `f25574fc` | pipeline#261 (blend kind dispatch) + #262 (the as-of provider loader orch#783 consumes) |

## Verification (candidate assembly, the #561 fakeroot procedure)

- Snapshot regenerated from a faithful runtime layout (s104 at the pin,
  pipeline 3ecd9880 for the canonical resolver, real artifact base);
  dry-run against the #85 branch tip showed the ONLY deltas are the pin
  table + machine-block pin + source fingerprints — the profile is not a
  snapshot source, exactly as expected.
- verify-pinned-declaration + candidate-pin artifact gate run in this
  PR's CI.

## After merge (deployment, recorded as the S1 boundary)

1. Live umbrella preflight (read-only) → ff-only pull (brings this lock +
   the Step 5b rail).
2. Runtime sync: s104 + pipeline checkouts to the two pins above.
3. THE RECORDED S1 DEPLOYMENT BOUNDARY (frozen prereg): the completed
   sync's timestamp + verified shas go in the grants trail and the s104
   profile PR's progress doc follow-up. The 20-session S1 clock starts at
   the FIRST SCHEDULED SESSION after that boundary; every scheduled
   session counts, including load-failed and record-less.
4. First expected behavior: daily_104.sh Step 5b finds the profile in the
   pinned configs and runs the S1 lane (tag alpaca_shadow_blend_mom);
   the momentum leg loads via the chain-verified ledger with the recipe
   pin momentum-v0-fd65161a20b29314.

## Rollback

Revert this commit; Step 5b returns to its INFO-skip dormant state (the
rail gates on the PINNED profile, so un-pinning fully deactivates the
lane); runtime checkouts re-sync to the reverted lock.
