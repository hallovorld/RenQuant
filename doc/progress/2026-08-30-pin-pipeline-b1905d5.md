# 2026-08-30 — pipeline pin advance to b1905d5

STATUS:    delivered (pin advance only; no runtime sync in this PR).

WHAT:      Advances the `renquant-pipeline` pin in `subrepos.lock.json`
           from `afb73626192f` to `b1905d5b3a55cb8773cdf5225dc9ccff3aef2432`.
           Four merged PRs are included:

           - **#306** `fix(ci): pin scs<3.3.0` — the 3.3.0 wheel kills the
             suite on the first infeasible QP.
           - **#303** `fix(kernel): unseen Q-state abstains instead of scoring
             0.0→positive ER; model_sell streak increments once per session
             (persisted)` — two root-cause fixes for trading noise.
           - **#305** `fix(preflight): never print a bare checkmark for a
             licensed P-WF-GATE or a relaxed P-REGIME-IC` — preflight honesty.
           - **#307** `fix(kernel): classification/xgboost per-ticker models
             abstain on NaN inputs (same trap as #303)` — the same 0.0→positive
             ER trap in the classification and xgboost model families.

WHY/DIRECTION:
           These four fixes are the pipeline half of the fix wave that stops
           unvalidated rotation churn (#303/#307 remove the positive-ER-from-
           nothing signal that drives net_adv comparisons) and makes the
           preflight tell the truth (#305). Without this pin advance, the live
           pipeline still maps unseen Q-states to 0.0 → positive ER via
           isotonic calibration, and classification/xgboost models do the same.

EVIDENCE:  Conviction guard 95/95 admits after bump [VERIFIED promote_pin.py
           --apply]. Pipeline CI green on all four PRs at merge time [VERIFIED
           gh pr checks]. No runtime sync in this PR — the live tree advances
           in a separate reviewed step.

NEXT:      (1) codex review; (2) merge; (3) live-tree ff including this pin;
           (4) Monday 2026-08-31 session uses the fixed pipeline.
