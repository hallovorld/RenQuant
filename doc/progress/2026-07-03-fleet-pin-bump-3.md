# Fleet pin bump 3 — sprint tail (st104 M6 flag config #42, orchestrator risk-budget #294)

STATUS: chore (lock-only), third and final bump of 2026-07-03. base-data/pipeline pins were
already advanced on main by the co-maintaining loops (rawlabel #33, provenance #34, stops
mirror — thank you); this bump adds the last two. Safety class identical to #438/#441:
the strategy-104 delta is an explicit-equals-default fingerprint key (merge-inert, pinned
by test); the orchestrator delta is the observe-only risk-budget ledger + sprint docs.
After this: the machine at pin-align runs the complete sprint output.

## Round 2 (CI fix)

`verify-pinned-declaration` failed: the committed `doc/arch/strategy-104-snapshot.md` was
still generated from the prior strategy-104 pin (`dd337d45`), not the `c5cd830e` this PR
bumps to — the snapshot generation step was skipped as part of the pin bump, same class of
issue as `RenQuant#438` earlier this session. Regenerated by materializing the pinned
commit at the script's actual default runtime path
(`.subrepo_runtime/repos/renquant-strategy-104/`) so `_read_pinned_checkout_head` resolves
the git HEAD correctly — a plain clone under an arbitrary directory name does not work here,
since `pinned_git_dir` always defaults to that fixed relative path regardless of what
`--configs-dir` is passed. Confirmed the regenerated snapshot also passes
`--verify-pinned-declaration` under CI's exact invocation (checkout named
`pinned-strategy-104/`, matching `strategy-104-snapshot-fresh.yml`). 204/205 relevant tests
pass; the 1 failure is the same pre-existing, unrelated cwd-sensitivity quirk confirmed
earlier this session.
