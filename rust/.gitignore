# Cargo build artifacts — same role as Python's __pycache__/, never committed.
/target/

# IDE / editor scratch.
.idea/
.vscode/

# OS noise.
.DS_Store

# Per-developer overrides (NOT shared in version control — same role as
# personal conda overrides). The committed config.toml is the team default.
.cargo/credentials
.cargo/credentials.toml
