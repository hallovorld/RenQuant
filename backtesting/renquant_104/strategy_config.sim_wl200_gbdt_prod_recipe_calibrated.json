{
  "_schema_note": "Minimal base config for run_wf_gate --derive-config-from-prod. Wrapper expects walkforward.manifest_path; all other semantics derived from strategy_config.json (prod). Created 2026-06-01 to replace the stale sim_wl200_172_sentiment.calibrated_causal.json that the wrapper referenced.",
  "walkforward": {
    "manifest_path": "artifacts/sim/walkforward_manifest_gbdt_prod_recipe_calibrated.json"
  }
}
